@model CoMan.Models.CooperationRequestModel

@{
    ViewData["Title"] = "Create cooperation request";
}

<h1>Apply</h1>

<h4>Send cooperation request</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudentComment" class="control-label"></label>
                <textarea asp-for="StudentComment" class="form-control"></textarea>
                <span asp-validation-for="StudentComment" class="text-danger"></span>
            </div>
             <div class="form-group">
                <label for="topic" class="control-label">Topic</label>
                <input type="text" id="topic" value="@ViewBag.TopicTitle" class="form-control" readonly />
            </div>
            <div class="form-group">
                <label for="student" class="control-label">Teacher</label>
                <input type="text" id="teacher" value="@ViewBag.TeacherEmail" class="form-control" readonly />
            </div>
            <input type="number" name="topicId" value="@ViewBag.TopicId" hidden />
            <input type="text" name="teacherId" value="@ViewBag.TeacherId" hidden />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-controller="Topic" >Back to list</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
